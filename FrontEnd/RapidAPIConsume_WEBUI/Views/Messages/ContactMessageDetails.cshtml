@model Contact
@{
    ViewData["Title"] = "ContactMessageDetails";
    Layout = "~/Views/AdminLayout/Layout.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                @await Component.InvokeAsync("AdminMailLeftBox")
                <div class="email-right-box">
                    <div class="toolbar" role="toolbar">
                        <div class="btn-group m-b-20">
                            <a href="/Message/DeleteContactMessage/@Model.ContactID" class="btn btn-light"><i class="fa fa-trash"></i></a>
                        </div>
                    

                    </div>
                    <div class="read-content">
                        <div class="media pt-5">
                            <img class="mr-3 rounded-circle" src="~/adminweb/images/avatar/7.jpg">
                            <div class="media-body">
                                <h5 class="m-b-3">@Model.Name</h5>
                                <p class="m-b-2">@(((DateTime)(Model.Date)).ToString("dd-MM-yyyy"))</p>
                            </div>
                                            
                        </div>
                        <hr>
                        <div class="media mb-4 mt-1">
                            <div class="media-body"><span class="float-right">@(((DateTime)(Model.Date)).ToString("HH:mm"))</span>
                                <h4 class="m-0 text-primary">@Model.Subject</h4><small class="text-muted">From: @Model.Mail</small>
                            </div>
                        </div>
                     
                        <p>@Model.Message
                        </p>
                        <p>@Model.Name</p>


                    </div>
                  
                </div>
            </div>
        </div>
    </div>
</div>

